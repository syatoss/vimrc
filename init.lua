require("options") -- all setters
require("plugins") -- all plugin defs (using packer)
require("keymaps") -- all standark key mappings (including plugins)
require("startups") -- turning on apps that require manual activision like treesitter
require("lsp") -- all lsp setput including keymaps
require("autocmd") -- commands that react to events
